import events from '../events.js';

// & description of module

const functionName = (() => {
    // data

    // cache DOM

    // bind eventListeners

    // methods

    // event subscriptions

    // make public
    return {
        
    }

})();

export default functionName;